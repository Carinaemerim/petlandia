spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:petlandia}:3306/${MYSQL_DATABASE:petlandia}
    platform: mysql
    username: ${MYSQL_USER:petlandia}
    password: ${MYSQL_PASSWORD:petlandia}
---
spring:
  profiles:
    active: development