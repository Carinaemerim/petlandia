spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  thymeleaf:
    cache: false
logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    org.hibernate: ERROR
  file:
    name: errors.log
    path: logs
server:
  port: 80
